dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "missing-gcloud-sdk"

include ':testing-v1'
include ':toolresults-v1beta3'
